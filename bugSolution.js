function foo(a, b) {
  if (a === null || b === null) {
    // Handle null values gracefully (e.g., return a default value or throw an error)
    return 0; //Returning a default value of 0
  }
  // ... rest of the function code
}